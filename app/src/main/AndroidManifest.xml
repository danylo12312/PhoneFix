<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PhoneFix"
        tools:targetApi="31">

        <!-- Екран авторизації — LAUNCHER -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.PhoneFix.Auth">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Головний екран -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Створення / редагування ордеру -->
        <activity
            android:name=".ui.repairs.NewRepairActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Деталі ремонту -->
        <activity
            android:name=".ui.repairs.RepairDetailActivity"
            android:exported="false" />

        <!-- Деталі клієнта -->
        <activity
            android:name=".ui.clients.ClientDetailActivity"
            android:exported="false" />

    </application>

</manifest>
