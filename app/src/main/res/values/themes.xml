<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.PhoneFix" parent="Theme.Material3.Light.NoActionBar">

        <!-- Primary -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorPrimaryContainer">@color/primary_container</item>
        <item name="colorOnPrimaryContainer">@color/primary</item>

        <!-- Secondary (використовуємо відтінки primary) -->
        <item name="colorSecondary">@color/primary_light</item>
        <item name="colorOnSecondary">@color/on_primary</item>

        <!-- Background & Surface -->
        <item name="android:colorBackground">@color/background</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorSurfaceVariant">@color/surface_variant</item>
        <item name="colorOnSurface">@color/on_surface</item>
        <item name="colorOnSurfaceVariant">@color/text_secondary</item>

        <!-- Error -->
        <item name="colorError">@color/error</item>

        <!-- Outline -->
        <item name="colorOutline">@color/outline</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@color/primary_dark</item>
        <item name="android:navigationBarColor">@color/surface</item>
        <item name="android:windowLightStatusBar">false</item>

    </style>

    <!-- Стиль для екрану авторизації (градієнтний фон) -->
    <style name="Theme.PhoneFix.Auth" parent="Theme.PhoneFix">
        <item name="android:statusBarColor">@color/gradient_start</item>
    </style>

</resources>
